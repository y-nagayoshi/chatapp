interface Props {
    currentUserId: string;
    accountId: string;
    accountType: string;
}

const ThreadsTab = async ({
    currentUserId,
    accountId,
    accountType
}:Props) => {

    
    return (
        <section>
            Thread tabs
        </section>
    )
}

export default ThreadsTab;